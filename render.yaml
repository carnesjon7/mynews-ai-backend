services:
  - type: web
    name: mynews-ai-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn mynews_ai_backend:app --bind 0.0.0.0:10000
    envVars:
      - key: PORT
        value: 10000
